{
  "dependencies": {
    "@types/webextension-polyfill": "^0.12.3",
    "webextension-polyfill": "^0.12.0"
  },
  "scripts": {
    "build": "bun build src/index.ts src/background.ts --outdir dist"
  }
}