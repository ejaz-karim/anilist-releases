# AniList Releases
Seadex and Nyaa integration to AniList entries.

## Building from source
Install [Bun](https://bun.com/) (any version works), Next run the command ```bun install``` then ```bun run build``` in the project directory.
